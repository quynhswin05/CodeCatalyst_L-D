def factorial(num):
    # not integer
    if type(num) != int:
        return None
    #negative
    if num < 0:
        return None
    #special case 0
    if num == 0:
        return 1
    
    return num * factorial(num -1)

    #factorial(3) ==> 3 * (2 * 1 * 1)
